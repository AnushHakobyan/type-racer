import React from 'react';
import Input from '../Input';

const KeyControlledInput = ({ textToMatch }) => {
    
    return (
        <Input onKeyPress={} />
    )
};

export default KeyControlledInput;
